# @dsh-trading/crypto —— crypto 市场 bundle 的 patch 层（insert-only，README 设计铁律 #1）。
#
# 架构修订（2026-08-29 主 agent 决策）：connector/kit 的插件行从 bundle patch 移到 preset
# 的 agent.cordis.yml（preset 级会话隔离——验收标准要求「普通会话看不到 crypto 工具」，
# tools/skills 注册表按 scope 分层，preset scope 内注册的工具不进入 standard 会话）。
# 本 bundle 的职责：
#   1. 依赖安装载体：preset 行按 bare 包名从 profile node_modules 解析（S3 坑 3），本包
#      dependencies 实装 @dsh-trading/connector-binance 与 @dsh-trading/kit-crypto，
#      保证 preset 行可解析、不 broken；
#   2. 未来共享行（agent-presets root 等统一配置归 base 所有，届时仍按 insert-only 插入）。
#
# 注意：patch 文件必须保持「顶层 YAML 数组」形状（loadProfile → parsePatchList 对非数组
# 直接抛错，纯注释文件解析为 null 会崩掉 profile 加载），空层写 `[]`。
[]
