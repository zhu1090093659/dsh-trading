/**
 * @dsh-trading/crypto — crypto 市场 bundle。包的实质是 `cordis.patch.yml`
 * （insert-only 聚合 connector-binance 与 kit-crypto 两行，铁律 #1），
 * 本模块无运行时 API（官方 bundle/base 形态）。
 *
 * @module @dsh-trading/crypto
 */

export {}
