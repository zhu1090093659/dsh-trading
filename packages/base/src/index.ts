/**
 * @dsh-trading/base — 市场无关核心 bundle。包的实质是 `cordis.patch.yml`
 * （由 `dsh.bundle.patch` manifest 字段声明、profile composer 解析），
 * 本模块无运行时 API（官方 bundle/base 形态：packages/bundle/base/src/index.ts）。
 *
 * @module @dsh-trading/base
 */

export {}
