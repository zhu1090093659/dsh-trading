/**
 * indicator-supertrend node half. Empty apply so the row appears in the
 * host cordis.yml / Loader (mounted via a profile-level patch row — the
 * community-plugin path); the browser half owns the indicator registration.
 */

/** Host plugin body — no host-side behavior. */
export function apply(): void {}
