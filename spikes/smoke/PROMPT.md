冒烟自检（只做以下三件事，输出尽量简短）：
1. 列出你当前可用的工具（tool）名单，明确指出其中是否存在名为 crypto_get_ticker 的工具。
2. 尝试调用工具 crypto_get_ticker，参数 {"symbol": "BTCUSDT"}，并原样报告调用返回的错误信息（预期可能报 not implemented，这也算结果）。
3. 列出你当前可加载的 skill（技能）名单，明确指出是否存在名为 crypto-risk-checklist 的技能；若存在，用一句话概括其用途。
不要做其他任何事，不要读写文件。
