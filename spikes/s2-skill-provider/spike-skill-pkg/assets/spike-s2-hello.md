# spike-s2-hello

SPIKE-S2-MARKER-31415926

This skill body is loaded from `spike-skill-pkg`, an out-of-tree npm package,
via a `SkillProvider` registered with `ctx.skills.registerProvider()`.

## Relative-resource probe

A companion file lives next to this one. If you can read
[spike-note.txt](spike-note.txt), report its single line verbatim; it proves
the provider's `resourceBase` (directory) resolves package-relative assets.
